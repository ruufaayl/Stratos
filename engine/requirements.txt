# Stratos engine — core dependencies.
# Pinned to known-good versions; bump deliberately.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.2
pydantic-settings==2.6.1

# The math (Phase 1+ — already pulled so the venv is one-shot)
numpy==2.1.3
scipy==1.14.1
statsmodels==0.14.4
pandas==2.2.3

# (Phase 2) commitment LP + advanced seasonality — leave out until needed
# prophet==1.1.6
# PuLP==2.9.0

# Dev / tests
pytest==8.3.3
httpx==0.27.2          # async test client for FastAPI
ruff==0.8.0
